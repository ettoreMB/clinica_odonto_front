import { styled } from "../../styles";

export  const Container = styled('div', {
  width: 'fit-content',
  gap: '1rem'
})
export const Box = styled("div", {
  display: 'inline-flex',
  alignItems: 'flex-end',
  gap:'1rem'
})

